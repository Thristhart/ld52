import { DecodedBuffer, Descriptor } from "buffer-backed-object/buffer-backed-object";

export function removeFromBufferBasedArray(array: Array<DecodedBuffer<{ type: Descriptor<number> }>>, index: number) {}
